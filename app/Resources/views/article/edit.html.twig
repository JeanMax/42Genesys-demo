{% extends 'base.html.twig' %}
{% import "macro.html.twig" as macro %}

{% block body %}
    <h1 class=my-4>Article edit</h1>

    {{ form_start(edit_form, {'attr': {'class': 'form-horizontal'}}) }}
        {{ form_errors(edit_form) }}
        {{macro.input('Auteur', edit_form.author)}}
        {{macro.input('Contenu', edit_form.content)}}
        {{macro.input('Images', edit_form.pictures)}}
        {{macro.submit('Edit')}}
    {{ form_end(edit_form) }}

    {{ form_start(delete_form) }}
        {{macro.submit('Delete')}}
    {{ form_end(delete_form) }}

    <a href="{{ path('article_show', { 'id': article.id }) }}">&larr; Back to the article</a>
{% endblock %}
